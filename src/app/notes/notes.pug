h1 Here is the Notes
.row
	.col-md-4.sidebar
		.left-notelist
			button.btn.btn-success(routerLink="addNote") Add new note 
			h4 My Notes
			ul.list-groups
				li.list-group-item(*ngFor=" let note of notes")
					a() {{ note.title }}
					p Date: 
						span {{ note.createdDate | date }}
					p {{ note.body }}
					p User: 
						span {{ note.user }}
					button.btn.btn-danger((click)="deleteNote(note)") Delete

	.col-md-8
		.well
			h2 Note details


